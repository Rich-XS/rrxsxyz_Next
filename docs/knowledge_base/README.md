# 知识库系统 - 完整使用指南

**版本**: 1.0
**创建时间**: 2025-11-06 03:30 (GMT+8)
**授权**: Queen授权P1及以下任务完全自主

---

## 📚 系统概述

**核心能力**:
- 从118+决策中自动提取知识（46个决策，8个模式，42个解决方案）
- 智能检索（关键词/模式/相似度）
- 自动推理解决方案（RCCM框架）
- A/B/C任务分类决策

**目录结构**:
```
docs/knowledge_base/
├── schema.json                    # 架构定义
├── decisions/index.json           # 46个决策（5类）
├── patterns/index.json            # 8个模式（含案例）
├── solutions/index.json           # 42个解决方案（5类）
├── constraints/index.json         # 15个约束（5类）
├── agents/capabilities.json       # Agent能力矩阵
├── retrieval_results.json         # 检索结果缓存
└── reasoning_results.json         # 推理结果缓存
```

---

## 🚀 快速开始

### 场景1: 查找历史类似问题

**问题**: "端口3001被占用，如何安全清理？"

**命令**:
```powershell
cd D:\OneDrive_New\_AIGPT\_100W_New\rrxsxyz_next
powershell -ExecutionPolicy Bypass -File scripts/retrieval_agent.ps1 -Query "端口冲突" -Mode "all"
```

**输出示例**:
```
📚 知识库检索Agent启动
查询: 端口冲突 | 模式: all | 最大结果数: 5

🔍 关键词搜索: 端口冲突
  → SOL-PROC-001: 端口冲突（3001/8080）
  → D-68: 跨项目端口保护

🎯 模式匹配: 端口冲突
  → PATTERN-4: Night-Auth自主执行 (匹配度: 2/5)

🔗 相似案例搜索: 端口冲突
  → D-79: 用户手动管理前后端服务 (相似度: 45.2%)

✅ 结果已保存到: docs/knowledge_base/retrieval_results.json
```

---

### 场景2: 自动推理解决方案

**问题**: "NUL文件突然增加到50个"

**命令**:
```powershell
powershell -ExecutionPolicy Bypass -File scripts/reasoning_agent.ps1 -Problem "NUL文件50个" -Severity "P1"
```

**推理过程**:
```
🧠 推理Agent启动
问题: NUL文件50个
严重级别: P1

📚 步骤1/5：检索知识库...
  → 找到3条相关决策（D-102, D-106, D-107）
  → 找到2个解决方案（SOL-FS-001, SOL-FS-002）

🎯 步骤2/5：模式识别...
  ✓ 识别到模式: RCCM（匹配度: 3/5）
  ✓ 识别到模式: 灾难→标准（匹配度: 2/5）

🔍 步骤3/5：根因分析（RCCM框架）...
  可能的根本原因:
  - 批处理 > nul + OneDrive + nodemon恶性循环 (HIGH)
  - NTFS MFT Cache内核内存 (MEDIUM)

💡 步骤4/5：生成解决方案推荐...

短期对策（P0，数分钟~数小时）:
  ID              Solution                                    Tool
  ------------    -----------------------------------------   ------------------------------------
  SOL-FS-001      禁用 > nul，使用 2>CON                      scripts/force_cleanup_nul.ps1

长期对策（P1-P2，数天~数周）:
  Type          Action                                        Enforcement
  ----------    ------------------------------------------    ----------------------------
  全局规则       提升为CLAUDE.md Rule系列                      Git pre-commit hook自动检查

🤔 步骤5/5：执行决策...
⏸ 等待用户确认...
建议任务类型: A 类（可自主执行）
```

**自动执行** (Night-Auth模式):
```powershell
powershell -ExecutionPolicy Bypass -File scripts/reasoning_agent.ps1 -Problem "NUL文件50个" -Severity "P1" -AutoExecute
```

---

## 🎯 使用场景速查

| 场景 | 命令 | 适用情况 |
|------|------|---------|
| **快速查找解决方案** | `retrieval_agent.ps1 -Query "关键词" -Mode keyword` | 已知问题关键词 |
| **识别问题模式** | `retrieval_agent.ps1 -Query "描述" -Mode pattern` | 需要识别属于哪类问题 |
| **查找类似案例** | `retrieval_agent.ps1 -Query "描述" -Mode similarity` | 寻找历史类似问题 |
| **全面分析** | `retrieval_agent.ps1 -Query "描述" -Mode all` | 综合检索（推荐） |
| **自动推理方案** | `reasoning_agent.ps1 -Problem "描述" -Severity "P1"` | 需要生成解决方案 |
| **自主执行（Night-Auth）** | `reasoning_agent.ps1 -Problem "描述" -AutoExecute` | P1任务自动执行 |

---

## 🔗 与现有工作流集成

### 集成1: progress-recorder Agent

**触发词**: `>>record`, `>>archive`, `>>wrap-up`

**集成点**: 每次记录决策时，自动更新知识库
```
用户完成D-XXX决策
  ↓
progress-recorder记录到progress.md
  ↓
自动触发知识库更新脚本
  ↓
decisions/index.json新增决策记录
  ↓
如识别到新模式 → patterns/index.json更新
```

### 集成2: RCCM框架

**触发词**: ` !RCCM` 或 `RCCM!`

**集成点**: RCCM分析时自动调用推理Agent
```
用户触发 !RCCM
  ↓
描述问题 → reasoning_agent.ps1
  ↓
检索知识库 + 模式识别 + 根因分析
  ↓
生成短期/长期对策 → 用户确认
  ↓
记录到progress.md Decisions
```

### 集成3: Gemba-Agent验证

**触发词**: Gemba测试失败时

**集成点**: 测试失败自动推理解决方案
```
Gemba-Agent检测到Bug
  ↓
提取错误描述（Console日志/截图）
  ↓
reasoning_agent.ps1分析
  ↓
推荐修复方案 → 自动修复（A类）或等待确认（B/C类）
```

---

## 📊 知识库统计

**数据规模**:
- **决策库**: 46个决策，5个类别（灾难恢复/系统优化/工具选型/架构设计/规范制定）
- **模式库**: 8个核心模式（RCCM/迭代诊断/三层同步/Night-Auth/灾难→标准/Kernel约束/版本自动化/工具进化）
- **解决方案库**: 42个已验证方案，5个类别（文件系统/进程管理/配置优化/网络同步/代码质量）
- **约束库**: 15个系统级约束，5个类别（OS级/工具限制/网络限制/权限限制/认知限制）
- **能力矩阵**: A/B/C三类任务分类 + 工具授权清单

**覆盖决策**:
- P0-CRITICAL: 8个（D-102/D-103/D-104/D-106/D-112/D-113等）
- P1-HIGH: 12个（D-68/D-77/D-85/D-95/D-97/D-98等）
- P2-MEDIUM: 26个（其他架构/规范/工具决策）

**典型案例**:
- D-102 (NUL文件灾难): 2,619个文件 → 完美根治（20WHY分析 + 全局规范）
- D-104 (配置膨胀): 862MB → 1.64MB（99.8%优化，4.5h Night-Auth）
- D-98 (Playwright迁移): 11文件1,960行代码（性能提升20-30%）

---

## ✅ 测试验证

### 测试1: 关键词检索准确率

**测试用例**: "NUL文件", "端口冲突", "版本号混淆", "OneDrive同步"

**结果**:
- NUL文件 → 准确命中 D-102, D-106, SOL-FS-001 ✅
- 端口冲突 → 准确命中 D-68, SOL-PROC-001 ✅
- 版本号混淆 → 准确命中 D-85, D-95, SOL-CFG-001 ✅
- OneDrive同步 → 准确命中 D-ONEDRIVE-DIAG-002, SOL-NET-001 ✅

**准确率**: 100% (4/4)

### 测试2: 模式识别准确率

**测试用例**:
- "需要根本原因分析" → 应识别RCCM框架 ✅
- "假设驱动诊断失败" → 应识别迭代诊断模式 ✅
- "文档与代码不一致" → 应识别三层同步模式 ✅
- "系统级问题需要重启" → 应识别Kernel约束模式 ✅

**准确率**: 100% (4/4)

### 测试3: 推理引擎完整性

**测试用例**: "端口3001被占用，启动失败"

**推理过程验证**:
1. ✅ 检索到相关决策（D-68, D-79）
2. ✅ 识别到模式（Night-Auth自主执行）
3. ✅ 推断根因（其他项目占用端口）
4. ✅ 生成短期对策（safe_port_cleanup.ps1）
5. ✅ 生成长期对策（跨项目端口隔离 D-65）
6. ✅ 正确分类为A类任务（可自主执行）

**完整性**: 100% (6/6步骤)

---

## 🔧 维护与更新

### 自动更新机制

**触发条件**:
1. progress.md新增决策（D-XXX）
2. 发现新的问题模式（重复发生3次+）
3. 验证新解决方案（P0/P1任务完成）

**更新流程**:
```powershell
# 手动更新知识库（从progress.md提取）
powershell -ExecutionPolicy Bypass -File scripts/update_knowledge_base.ps1

# 验证更新
powershell -ExecutionPolicy Bypass -File scripts/retrieval_agent.ps1 -Query "最新" -Mode keyword
```

### 定期维护

**每周维护** (配合weekly_config_cleanup.ps1):
1. 检查新增决策是否遗漏
2. 更新统计数据（total_decisions/total_patterns）
3. 归档过时约束（已解决的系统级问题）
4. 验证检索准确率（抽样测试）

---

## 🎓 学习案例

### 案例1: D-102 NUL文件灾难 → 全局规范

**问题**: 2,619个NUL文件污染文件系统

**知识库记录**:
- **决策**: D-102 (灾难恢复类，P0-CRITICAL)
- **模式**: RCCM框架 + 灾难→标准
- **解决方案**: SOL-FS-001 (禁用 > nul，使用 2>CON)
- **约束**: CONSTRAINT-OS-003 (批处理 > nul在复杂环境失效)
- **长期对策**: CLAUDE.md Rule 12（全局强制执行）

**如何检索**:
```powershell
retrieval_agent.ps1 -Query "批处理重定向" -Mode all
  → 准确命中D-102和SOL-FS-001
  → 识别到"灾难→标准"模式
  → 推荐全局规范解决方案
```

### 案例2: D-98 工具链进化（Playwright迁移）

**问题**: Puppeteer性能瓶颈

**知识库记录**:
- **决策**: D-98 (工具选型类，P1)
- **模式**: 工具链进化（评估→迁移→优化）
- **解决方案**: 性能提升20-30% + 多浏览器支持
- **向后兼容**: 保留Puppeteer版本

**如何检索**:
```powershell
retrieval_agent.ps1 -Query "浏览器自动化性能" -Mode similarity
  → 相似案例: D-77 (Gemba-Agent Puppeteer版本)
  → 推荐升级: D-98 (Playwright迁移)
  → 工具对比: 性能/兼容性/成本分析
```

---

## 🚨 常见问题

**Q1: 检索结果为空怎么办？**

A: 检查查询关键词是否准确，尝试使用更广泛的词语
```powershell
# ❌ 太具体
retrieval_agent.ps1 -Query "Code.exe进程循环" -Mode keyword

# ✅ 更广泛
retrieval_agent.ps1 -Query "CLI循环" -Mode all
```

**Q2: 推理Agent建议错误方案？**

A: 使用 `-Severity` 参数明确问题严重级别
```powershell
# P0级别 → 优先系统级解决方案
reasoning_agent.ps1 -Problem "系统无法启动" -Severity "P0"

# P2级别 → 优先轻量级方案
reasoning_agent.ps1 -Problem "UI显示小问题" -Severity "P2"
```

**Q3: 如何添加自定义模式？**

A: 手动编辑 `docs/knowledge_base/patterns/index.json`
```json
{
  "id": "PATTERN-9",
  "name": "自定义模式名称",
  "description": "模式描述",
  "workflow": ["步骤1", "步骤2", "步骤3"],
  "cases": [...]
}
```

---

## 📞 支持与反馈

**文档位置**: `docs/knowledge_base/README.md`
**脚本位置**: `scripts/retrieval_agent.ps1`, `scripts/reasoning_agent.ps1`
**知识库位置**: `docs/knowledge_base/`
**更新日志**: `progress.md` Decisions区块

**Night-Auth授权**: Queen授权P1及以下任务完全自主
**关联决策**: D-53 (三层同步), Night-Auth协议 (2025-10-10)

---

**版本**: 1.0 | **最后更新**: 2025-11-06 03:30 (GMT+8)
