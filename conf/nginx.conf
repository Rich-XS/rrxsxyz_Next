# Nginx Configuration for RRXS.XYZ
# ⚠️ 路径说明：本配置文件包含示例路径，部署时需要替换为实际路径
# 📁 参见：../PATH_NOTICE.md 了解路径管理规范

worker_processes  1;

# 将 PID 文件和日志都放到项目外部，避免项目目录被占用
# ⚠️ 部署时修改：替换为你的实际路径
pid D:/_100W/Logs/nginx.pid;

events {
    worker_connections  1024;
}

http {
    # ⚠️ 部署时修改：替换为你的实际项目路径
    include       D:/_100W/rrxsxyz_next/conf/mime.types;
    default_type  application/octet-stream;

    # 临时文件路径（项目内，打包时可排除）
    # ⚠️ 部署时修改：替换为你的实际项目路径
    client_body_temp_path  D:/_100W/rrxsxyz_next/temp/client_body_temp;

    server {
        listen       80;
        server_name  rrxs.xyz localhost;

        # ⚠️ 部署时修改：所有 root 路径需替换为你的实际项目路径
        location / {
            root   D:\_100W\rrxsxyz_next;
            index  index.html index.htm;
        }

        location /shop {
            root   D:\_100W\rrxsxyz_next;
            index  shop.html;
        }

        location /webbot {
            root   D:\_100W\rrxsxyz_next;
            index  webbot.html;
        }

        location /duomotai/ {
            root   D:\_100W\rrxsxyz_next;
            index  index.html;
        }

        # 日志路径
        error_log  D:\_100W\Logs\error.log;
        access_log D:\_100W\Logs\access.log;
    }
}
