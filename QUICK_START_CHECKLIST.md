# ⚡ 快速启动清单 - 继续 Night-Auth 工作

**用途**: 无间断工作模式下的快速恢复清单  
**更新时间**: 2025-10-24 18:15  

---

## 🚀 恢复服务（3 分钟）

### ✅ 检查服务状态
```bash
# 检查后端
curl http://localhost:3001/health

# 检查前端
curl http://localhost:8080/duomotai/index.html -I
```

**如果服务未运行**：
```bash
# 后端启动
cd D:\_100W\rrxsxyz_next\server
npm run dev

# 前端启动（新终端）
cd D:\_100W\rrxsxyz_next
python -m http.server 8080
```

---

## 📊 立即执行的任务顺序

### 任务序列 1：Gemba 完整测试（1 分钟启动 + 2 分钟观察）
```bash
cd D:\_100W\rrxsxyz_next
rm -rf gemba-reports  # 清理旧报告
node scripts/gemba-agent.js 2>&1 | tee gemba-test-$(date +%Y%m%d_%H%M).log
```

**观察要点**：
- [ ] 是否出现致命错误
- [ ] Console 中是否有 "🔍 [DebateEngine] 测试用户模式: ON"
- [ ] 是否在 30 秒内完成
- [ ] 生成的报告位置

### 任务序列 2：查看报告（1 分钟）
```bash
# 打开 HTML 报告
start ./gemba-reports/gemba-report.html
```

**核心指标**：
- 通过/失败测试数
- 是否有新错误
- Console 摘要中的关键信息

### 任务序列 3：对比分析（3 分钟）
```bash
# 编辑对比文档
code V54_vs_V55_ANALYSIS.md

# 关键更新内容：
# 1. 将"待验证问题"部分更新为"已验证"或"确认回归"
# 2. 添加本次 Gemba 测试的新发现
# 3. 为 P0 修复重新排序优先级
```

---

## 📋 当前文件状态速查

| 文件 | 最后修改 | 用途 | 操作 |
|------|---------|------|------|
| scripts/gemba-agent.js | 18:05 | 自动测试 | ✅ 已修复 waitForTimeout 错误 |
| debateEngine.js | 17:00+ | 核心逻辑 | ✅ D-76 已应用 |
| progress.md | 18:10 | 决策记录 | ✅ 已更新 D-76 成就 |
| V54_vs_V55_ANALYSIS.md | 18:00 | 对比文档 | ⏳ 待更新测试结果 |
| gemba-reports/ | 18:10 | 测试报告 | ✅ 已生成 |

---

## 🎯 核心决策点

### 如果 Gemba 测试通过（4/4）
```
→ 进行 P1 任务：根据 PRD 实施 P0 修复
→ 重点关注：专家发言字数、语音输出、内容递进式
```

### 如果 Gemba 测试失败
```
→ 检查 console 错误
→ 可能需要回滚某些修改
→ 参考 V54 版本进行对比修复
```

### 如果发现新 bug
```
→ 记录到 V54_vs_V55_ANALYSIS.md
→ 调用 progress-recorder 记录到 progress.md
→ 根据优先级决定是否立即修复
```

---

## 💾 备份策略（遵循 D-35 决策）

**P0 任务完成后**：
```bash
# 创建版本备份（Exclude 模式）
powershell -ExecutionPolicy Bypass -File scripts/TaskDone_BackUp_Exclude.ps1 \
  -Keyword "1024V55_D76_Verified" -Execute

# 新建档案记录
>>record  # 调用 progress-recorder 记录
```

---

## 🔗 关键参考文档

**立即阅读**（恢复上下文）：
- [ ] HANDOFF_TO_NEXT_SESSION.md（完整任务清单）
- [ ] V54_vs_V55_ANALYSIS.md（差异分析）

**任务执行参考**：
- [ ] PRD_DebateFlow_v1.md（P0 问题定义）
- [ ] progress.md（决策记录）

**问题排查参考**：
- [ ] ideas.md L234-249（原始需求）
- [ ] duomotai/ 目录结构

---

## ⚠️ 常见问题速解

### Q: 服务启动很慢？
A: 检查 npm 依赖是否完整
```bash
cd server
npm install  # 重新安装依赖
npm run dev
```

### Q: Gemba 报告在哪？
A: `D:\_100W\rrxsxyz_next\gemba-reports\gemba-report.html`

### Q: 如何对比 V54 和 V55？
A: 使用 VS Code 的 Diff
```bash
# 在 VS Code 中打开两个文件夹
# 文件夹 1: D:\_100W\rrxsxyz_next_20251023_2317_V54
# 文件夹 2: D:\_100W\rrxsxyz_next

# 对比关键文件：debateEngine.js, init.js, index.html
```

### Q: progress.md 已经太长？
A: 已设置自动 archive（80 行阈值）

---

## 🎬 标准开局流程（复制粘贴）

```bash
# 1. 进入项目目录
cd D:\_100W\rrxsxyz_next

# 2. 验证服务
curl http://localhost:3001/health
echo "---"
curl -s http://localhost:8080/ -o /dev/null && echo "✅ Frontend OK"

# 3. 清理旧报告
rm -rf gemba-reports

# 4. 运行 Gemba 测试（核心任务）
node scripts/gemba-agent.js

# 5. 查看报告
echo "✅ 测试完成，打开报告："
echo "   Path: ./gemba-reports/gemba-report.html"
```

---

## 📞 紧急联系

**如果遇到阻塞**：
- 检查 progress.md 的最新决策
- 查看 CLAUDE.md 的 Rule 5（D-68 端口保护）
- 参考 HANDOFF_TO_NEXT_SESSION.md 的"建议工作顺序"

**关键缩写**：
- P0: 必须立即做
- P1: 高优先级
- P2: 可选优化
- Night-Auth: 无间断工作模式（当前启用）

---

**最后更新**: 2025-10-24 18:15  
**状态**: 🟢 准备就绪  
**下一步**: 运行 Gemba 测试

